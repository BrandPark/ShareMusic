import React, { Component } from 'react';

class Test extends Component {
    onTest() {
        // login rest api 호출
        
    }
    render() {
        return (
            <button onClick={onTest}></button>
        );
    }
}

export default Test;